kind = "web"
previewPath = "/"
title = "ProofStore"
version = "1.0.0"
id = "artifacts/proofstore"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20174

[services.development]
run = "pnpm --filter @workspace/proofstore run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/proofstore", "run", "build" ]
publicDir = "artifacts/proofstore/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20174"
BASE_PATH = "/"
